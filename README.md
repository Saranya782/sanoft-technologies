# Sanoft Technologies

## Project Overview
[Briefly describe what this application does, its main purpose, and the problem it solves.]

## Architecture
- **Frontend**: [React/Next.js/etc.]
- **Backend**: [Node.js/Express/etc.]
- **Database**: [MongoDB/PostgreSQL/etc.]
*(Optional: Insert architecture diagram link here)*

## Setup & Local Development Instructions

### Prerequisites
- Node.js (v18+)
- npm or yarn

### Frontend Setup
```bash
cd frontend
npm install
npm run dev
```

### Backend Setup
```bash
cd backend
npm install
npm run dev
```

## Environment Variable Documentation

### Frontend (`frontend/.env`)
```env
VITE_API_URL=http://localhost:5000/api
# Add other frontend env variables here
```

### Backend (`backend/.env`)
```env
PORT=5000
DATABASE_URL=your_database_url
# Add other backend env variables here
```

## Deployment Steps
1. **Frontend Deployment (e.g., Vercel/Netlify)**:
   - Connect the repository to your hosting provider.
   - Set the build command to `npm run build` and output directory to `dist` or `build`.
   - Add frontend environment variables.

2. **Backend Deployment (e.g., Render/Railway)**:
   - Connect the repository.
   - Set the root directory to `backend`.
   - Add backend environment variables.

## Live URLs
- **Frontend Live URL**: [Pending]
- **Backend Live URL**: [Pending]
